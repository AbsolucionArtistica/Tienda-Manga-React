# API Configuration - Backend MySQL/Sequelize
# Actualizar si el backend está en un puerto o host diferente
VITE_API_URL=http://localhost:5000/api

# Application Configuration
VITE_APP_NAME=MangoManga
VITE_APP_VERSION=1.0.0

# Notes:
# - El frontend no necesita cambios significativos tras la migración MongoDB -> MySQL
# - Los endpoints son exactamente iguales
# - Solo verificar que el backend está corriendo en el puerto correcto
# - Si hay errores, revisar consola (F12) para detalles
